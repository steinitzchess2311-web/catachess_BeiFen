{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && ../.venv/bin/python -m uvicorn app.app:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health"
  }
}
