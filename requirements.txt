# ---- core ----
pydantic>=1.10,<3.0
pydantic-settings>=2.0
python-dotenv>=1.0
typing-extensions>=4.7

# ---- http ----
requests>=2.31
httpx>=0.27  # For TestClient

# ---- web ----
fastapi>=0.110
uvicorn>=0.29
gunicorn>=21.0  # Multi-worker process manager

# ---- database ----
sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9
asyncpg>=0.29

# MongoDB (Engine Cache)
motor>=3.3  # Async MongoDB driver for FastAPI
pymongo>=4.6  # MongoDB driver (required by motor)

# Redis (Blog Cache)
redis>=5.0  # Async Redis client for blog caching

# ---- security ----
bcrypt>=4.0
python-jose[cryptography]>=3.3
python-multipart>=0.0.6
cryptography>=42.0

# ---- identifiers / utils ----
python-ulid>=2.7.0   # provides: from ulid import ULID

# ---- email ----
resend>=0.8.0

# ---- storage ----
boto3>=1.34  # Cloudflare R2 / S3-compatible storage

# ---- image processing ----
Pillow>=10.0  # Image resizing and compression

python-ulid>=2.7.0
emoji>=2.0
python-chess>=1.999
